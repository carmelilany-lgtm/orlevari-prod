/** `sizes` hints so the Vercel image CDN serves a tile-sized file, not the original still. */
export const STILL_TILE_SIZES =
  "(max-width: 640px) 50vw, (max-width: 1024px) 33vw, 420px";

export const HERO_FEATURE_SIZES = "(max-width: 768px) 90vw, 720px";

export const HERO_CELL_SIZES = "(max-width: 768px) 45vw, 280px";

export const LIGHTBOX_SIZES = "(max-width: 1200px) 100vw, 1200px";

export const ABOUT_IMAGE_SIZES = "(max-width: 1024px) 260px, 420px";

export const VIDEO_THUMB_SIZES =
  "(max-width: 768px) 100vw, (max-width: 1280px) 50vw, 33vw";
