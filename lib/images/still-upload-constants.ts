/** Max still images per admin batch upload. */
export const MAX_BULK_STILL_UPLOAD_FILES = 40;

/** Parallel uploads per batch (avoids timeouts and overload). */
export const STILL_UPLOAD_CONCURRENCY = 4;
